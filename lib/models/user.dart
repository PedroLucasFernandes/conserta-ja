class User {
  String? email;
  String? phone;
  String? password;
  String? name;
  String? birthday;
  int? isClient;
  String? profileImage;

  User({this.email, this.phone, this.password, this.name, this.birthday, this.isClient, this.profileImage});
}